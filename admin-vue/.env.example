# admin-vue 环境变量示例
# 复制为 .env.development 或 .env.production 后填值

# 后端 API 地址(vite 代理时填 /api, 直连时填 https://api.cakeshop.com)
VITE_API_BASE=/api

# 内部 RPC Token(对应后端 INTERNAL_RPC_TOKEN)
# 用于云函数中转 / 跨服务调用
VITE_RPC_TOKEN=dev-internal-rpc-token-change-me-32-chars-min
